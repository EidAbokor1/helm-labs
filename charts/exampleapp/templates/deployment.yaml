{{- include "mylibcharts.deployment" (list . "exampleapp.deployment") -}}
{{- define "exampleapp.deployment" -}}
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
{{- end -}}